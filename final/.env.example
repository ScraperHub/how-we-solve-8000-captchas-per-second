# Crawlbase token, used only by the `-solver crawlbase` end-to-end path.
# The mock solver needs no token. Get yours at https://crawlbase.com/signup?signup=blog
CRAWLBASE_TOKEN=your_token_here

# URL the crawlbase solver fetches per challenge. example.com is safe to hit.
TARGET_URL=https://example.com
